ncpio:
  - type: api
    i_rules:
      - regexp: '.*'
    o_rules:
      - regexp: '.*'
  - type: tcps
    params: "localhost:1222"
    i_rules:
      - regexp: '.*'
    o_rules:
      - regexp: '.*'
  - type: mqtt
    params: "test_mqttd.yml"
    i_rules:
      - regexp: '.*'
    o_rules:
      - regexp: '.*'

